# 稠密重建各模块实现方法综述

本文档深入分析MVS稠密重建中各个关键模块的不同实现方式，以openMVS和ACMMP为主要参考，同时涵盖业界其他常见方法。

---

## 目录
1. [传播模式 (Propagation Pattern)](#1-传播模式-propagation-pattern)
2. [视图选择 (View Selection)](#2-视图选择-view-selection)
3. [几何一致性 (Geometric Consistency)](#3-几何一致性-geometric-consistency)
4. [平面先验 (Planar Prior)](#4-平面先验-planar-prior)
5. [匹配代价计算 (Matching Cost)](#5-匹配代价计算-matching-cost)
6. [邻域传播策略 (Neighbor Propagation)](#6-邻域传播策略-neighbor-propagation)
7. [搜索与优化策略 (Search Strategy)](#7-搜索与优化策略-search-strategy)
8. [多分辨率处理 (Multi-Resolution)](#8-多分辨率处理-multi-resolution)
9. [深度图融合 (Depth Map Fusion)](#9-深度图融合-depth-map-fusion)

---

## 1. 传播模式 (Propagation Pattern)

传播模式决定了PatchMatch算法中假设如何从一个像素传递到另一个像素。

### 1.1 顺序扫描传播 (Sequential Scanline)

**代表方法**: openMVS, 原始PatchMatch Stereo

**原理**: 按行或列顺序处理像素，每个像素从已处理的相邻像素获取假设。

**openMVS实现** (`DepthMap.cpp:329-356`):
```cpp
// 锯齿形扫描 - 创建像素处理顺序
void DepthEstimator::MapMatrix2ZigzagIdx(const Image8U::Size& size,
    DepthEstimator::MapRefArr& coords, const BitMatrix& mask, int rawStride)
{
    // 产生锯齿形索引：
    //                     1 2 3
    //  1 2 4 7 5 3 6 8 9 -->  4 5 6
    //                     7 8 9
    for (int dy=0, h=rawStride; dy<size.height; dy+=h) {
        int lastX = 0;
        MapRef x(MapRef::ZERO);
        for (int i=0, ei=w*h; i<ei; ++i) {
            const MapRef pt(x.x, x.y+dy);
            if (mask.empty() || mask.isSet(pt))
                coords.Insert(pt);
            if (x.x-- == 0 || ++x.y == h) {
                if (++lastX < w) { x.x = lastX; x.y = 0; }
                else { x.x = w1; x.y = lastX - w1; }
            }
        }
    }
}
```

**方向交替**:
```cpp
// 奇偶迭代交替方向
dir(nIter%2 ? RB2LT : LT2RB)  // 右下到左上 或 左上到右下
```

**优点**:
- 实现简单，内存访问连续
- 适合CPU多线程（按行分配）
- 收敛稳定

**缺点**:
- 并行度受限（存在数据依赖）
- GPU实现效率较低

---

### 1.2 棋盘格并行传播 (Checkerboard Propagation)

**代表方法**: ACMMP, Gipuma, COLMAP

**原理**: 将图像分为红黑两组（如棋盘），同色像素间无数据依赖，可完全并行。

**ACMMP实现** (`ACMMP.cu:737-999`):
```cuda
__device__ void CheckerboardPropagation(
    const cudaTextureObject_t *images,
    const cudaTextureObject_t *depths,
    const Camera *cameras,
    float4 *plane_hypotheses,
    float *costs,
    const int2 p,
    const PatchMatchParams params,
    const int iter)
{
    const int center = p.y * width + p.x;

    // 8邻域定义：近邻(±1) + 远邻(±3)
    int left_near = center - 1;
    int left_far = center - 3;
    int right_near = center + 1;
    int right_far = center + 3;
    int up_near = center - width;
    int up_far = center - 3 * width;
    int down_near = center + width;
    int down_far = center + 3 * width;

    // 对每个方向，搜索最小代价的邻居
    // 例如: up_far方向搜索范围扩展到±22像素
    if (p.y > 2) {
        costMin = costs[up_far];
        costMinPoint = up_far;
        for (int i = 1; i < 11; ++i) {
            if (p.y > 2 + 2 * i) {
                int pointTemp = up_far - 2 * i * width;
                if (costs[pointTemp] < costMin) {
                    costMin = costs[pointTemp];
                    costMinPoint = pointTemp;
                }
            }
        }
        up_far = costMinPoint;  // 自适应选择最优远邻居
    }
    // ... 其他方向类似
}
```

**棋盘格迭代模式**:
```
迭代1: 更新红色格子（黑色格子作为源）
  ■ □ ■ □ ■
  □ ■ □ ■ □
  ■ □ ■ □ ■

迭代2: 更新黑色格子（红色格子作为源）
  □ ■ □ ■ □
  ■ □ ■ □ ■
  □ ■ □ ■ □
```

**CUDA kernel调用**:
```cuda
// 红色格子
if ((p.x + p.y) % 2 == iter % 2) {
    CheckerboardPropagation(...);
}
```

**优点**:
- GPU高度并行，吞吐量大
- 每次迭代更新50%像素

**缺点**:
- 传播距离短（需要更多迭代）
- 实现复杂度高

---

### 1.3 扩散传播 (Diffusion-like Propagation)

**代表方法**: 某些优化方法

**原理**: 模拟热扩散方程，假设通过迭代逐渐扩散到整个图像。

```
∂d/∂t = α∇²d + f(d, neighbors)
```

**特点**: 更平滑的收敛，但速度较慢。

---

### 1.4 传播模式对比总结

| 特性 | 顺序扫描(openMVS) | 棋盘格(ACMMP) | 扩散式 |
|-----|-----------------|--------------|--------|
| **并行度** | 低-中 | 高 | 中 |
| **收敛速度** | 快 | 中 | 慢 |
| **实现平台** | CPU友好 | GPU友好 | 通用 |
| **内存访问** | 连续 | 跳跃 | 局部 |
| **迭代次数** | 3-4次 | 8-12次 | 10+次 |

---

## 2. 视图选择 (View Selection)

视图选择决定使用哪些源视图来估计参考视图的深度。

### 2.1 静态预选择 + MRF优化 (openMVS)

**流程**:
1. **局部评分**: 计算每对视图的共视质量
2. **全局优化**: MRF能量最小化选择最优子集

**评分函数** (`SceneDensify.cpp`):
```cpp
// 视图对评分基于：
// 1. 共视点数量
// 2. 基线角度 (理想: 12-15度)
// 3. 尺度一致性
// 4. 遮挡分析

float ComputeViewScore(const View& ref, const View& src) {
    float score = 0;

    // 共视点贡献
    score += numSharedPoints * pointWeight;

    // 角度贡献 (三角形模型)
    float angle = ComputeBaseline Angle(ref, src);
    if (angle >= fMinAngle && angle <= fMaxAngle)
        score += (1 - abs(angle - fOptimAngle) / fMaxAngle) * angleWeight;

    // 尺度贡献
    float scaleRatio = ref.scale / src.scale;
    if (scaleRatio >= 0.5 && scaleRatio <= 2.0)
        score += scaleWeight;

    return score;
}
```

**MRF全局优化**:
```
E(V) = Σᵢ ψᵢ(vᵢ) + λ Σᵢⱼ ψᵢⱼ(vᵢ, vⱼ)

ψᵢ(vᵢ) = -log(score_i)           // 一元项: 视图质量
ψᵢⱼ(vᵢ, vⱼ) = overlap(i,j) * diff(vᵢ, vⱼ)  // 二元项: 视图互补性
```

**优点**: 全局最优，视图分布均匀
**缺点**: 预处理开销大，无法适应局部变化

---

### 2.2 动态自适应视图选择 (ACMMP)

**原理**: 在PatchMatch迭代过程中，根据匹配质量动态选择视图。

**ACMMP实现** (`ACMMP.cu:423-460`):
```cuda
__device__ float ComputeMultiViewInitialCostandSelectedViews(
    const cudaTextureObject_t *images,
    const Camera *cameras,
    const int2 p,
    const float4 plane_hypothesis,
    unsigned int *selected_views,
    const PatchMatchParams params)
{
    float cost_vector[32] = {2.0f};
    float cost_vector_copy[32] = {2.0f};
    int num_valid_views = 0;

    // 计算所有视图的匹配代价
    for (int i = 1; i < params.num_images; ++i) {
        float c = ComputeBilateralNCC(images[0], cameras[0],
                                       images[i], cameras[i],
                                       p, plane_hypothesis, params);
        cost_vector[i - 1] = c;
        cost_vector_copy[i - 1] = c;
        if (c < 2.0f) num_valid_views++;
    }

    // 排序选择top-k个最佳视图
    sort_small(cost_vector, cost_count);
    *selected_views = 0;

    int top_k = min(num_valid_views, params.top_k);
    if (top_k > 0) {
        float cost = 0.0f;
        float cost_threshold = cost_vector[top_k - 1];

        // 标记被选中的视图
        for (int i = 0; i < params.num_images - 1; ++i) {
            if (cost_vector_copy[i] <= cost_threshold) {
                setBit(*selected_views, i);  // 位图标记
            }
            cost += cost_vector[i];
        }
        return cost / top_k;
    }
    return 2.0f;  // 无效
}
```

**多假设联合视图选择** (`ACMMP.cu:946-996`):
```cuda
// 综合8个邻居的视图选择先验
float view_selection_priors[32] = {0.0f};
int neighbor_positions[4] = {center - width, center + width, center - 1, center + 1};

for (int i = 0; i < 4; ++i) {
    if (flag[2 * i]) {
        for (int j = 0; j < params.num_images - 1; ++j) {
            if (isSet(selected_views[neighbor_positions[i]], j) == 1) {
                view_selection_priors[j] += 0.9f;  // 邻居选了该视图
            } else {
                view_selection_priors[j] += 0.1f;  // 邻居未选该视图
            }
        }
    }
}

// 基于代价的采样概率
float cost_threshold = 0.8 * expf((iter) * (iter) / (-90.0f));
for (int i = 0; i < params.num_images - 1; i++) {
    float count = 0, tmpw = 0;
    for (int j = 0; j < 8; j++) {
        if (cost_array[j][i] < cost_threshold) {
            tmpw += expf(cost_array[j][i] * cost_array[j][i] / (-0.18f));
            count++;
        }
    }
    sampling_probs[i] = (tmpw / count) * view_selection_priors[i];
}

// 概率采样15次
TransformPDFToCDF(sampling_probs, params.num_images - 1);
for (int sample = 0; sample < 15; ++sample) {
    const float rand_prob = curand_uniform(&rand_states[center]);
    for (int image_id = 0; image_id < params.num_images - 1; ++image_id) {
        if (sampling_probs[image_id] > rand_prob) {
            view_weights[image_id] += 1.0f;
            break;
        }
    }
}
```

**优点**: 自适应处理遮挡和纹理变化
**缺点**: 计算开销大，需要存储视图选择位图

---

### 2.3 基于学习的视图选择

**代表方法**: MVSNet, PatchmatchNet

**原理**: 使用神经网络预测每个视图的重要性权重。

```python
# 伪代码
view_weights = ViewSelectionNet(ref_features, src_features)
aggregated_cost = sum(view_weights * costs) / sum(view_weights)
```

---

### 2.4 视图选择方法对比

| 方法 | 计算时机 | 自适应性 | 计算开销 | 遮挡处理 |
|-----|---------|---------|---------|---------|
| MRF全局(openMVS) | 预处理 | 差 | 一次性高 | 差 |
| 动态Top-K(ACMMP) | 运行时 | 好 | 每像素 | 好 |
| 概率采样(ACMMP) | 运行时 | 很好 | 高 | 很好 |
| 神经网络 | 运行时 | 很好 | GPU | 很好 |

---

## 3. 几何一致性 (Geometric Consistency)

几何一致性确保不同视图估计的深度彼此一致。

### 3.1 后处理几何一致性 (openMVS)

**原理**: 深度估计完成后，通过多视图投影验证一致性。

**openMVS实现流程**:
```cpp
// 1. 对每个参考视图的深度图
// 2. 投影到其他视图
// 3. 比较深度差异
// 4. 滤除不一致点

bool CheckGeometricConsistency(
    const DepthMap& refDepth,
    const DepthMap& srcDepth,
    const Camera& refCam,
    const Camera& srcCam,
    Point2i refPt)
{
    float d_ref = refDepth(refPt);
    Point3 X = refCam.Unproject(refPt, d_ref);  // 参考视图3D点

    Point2 srcPt = srcCam.Project(X);           // 投影到源视图
    float d_src_expected = srcCam.Depth(X);     // 期望深度
    float d_src_actual = srcDepth(srcPt);       // 实际深度

    float depth_diff = abs(d_src_expected - d_src_actual) / d_src_expected;
    return depth_diff < threshold;  // 默认0.01
}
```

**多视图一致性过滤**:
```cpp
// 需要至少N个视图一致才保留该点
int consistent_count = 0;
for (auto& srcView : neighborViews) {
    if (CheckGeometricConsistency(...))
        consistent_count++;
}
if (consistent_count >= nMinViews)
    keep_point = true;
```

---

### 3.2 集成到代价函数 (ACMMP)

**原理**: 将几何一致性作为代价的一部分，在迭代中优化。

**ACMMP实现** (`ACMMP.cu:507-532`):
```cuda
__device__ float ComputeGeomConsistencyCost(
    const cudaTextureObject_t depth_image,
    const Camera ref_camera,
    const Camera src_camera,
    const float4 plane_hypothesis,
    const int2 p)
{
    const float max_cost = 3.0f;

    // 1. 计算参考视图深度
    float depth = ComputeDepthfromPlaneHypothesis(ref_camera, plane_hypothesis, p);

    // 2. 反投影到世界坐标
    float3 forward_point = Get3DPointonWorld_cu(p.x, p.y, depth, ref_camera);

    // 3. 前向投影到源视图
    float2 src_pt;
    float src_d;
    ProjectonCamera_cu(forward_point, src_camera, src_pt, src_d);
    const float src_depth = tex2D<float>(depth_image, src_pt.x + 0.5f, src_pt.y + 0.5f);

    if (src_depth == 0.0f) return max_cost;

    // 4. 从源视图深度反投影到世界坐标
    float3 src_3D_pt = Get3DPointonWorld_cu(src_pt.x, src_pt.y, src_depth, src_camera);

    // 5. 反向投影回参考视图
    float2 backward_point;
    float ref_d;
    ProjectonCamera_cu(src_3D_pt, ref_camera, backward_point, ref_d);

    // 6. 计算重投影误差
    const float diff_col = p.x - backward_point.x;
    const float diff_row = p.y - backward_point.y;
    return min(max_cost, sqrt(diff_col * diff_col + diff_row * diff_row));
}
```

**代价融合**:
```cuda
// 在PlaneHypothesisRefinement中
if (params.geom_consistency) {
    temp_cost += view_weights[j] * (
        cost_vector[j] +  // 光度一致性
        0.2f * ComputeGeomConsistencyCost(...)  // 几何一致性，权重0.2
    );
}
```

---

### 3.3 多尺度几何一致性 (ACMMP)

**原理**: 在多个分辨率层级上进行几何一致性检查。

```
层级3 (1/8分辨率): 初始估计
    ↓ 上采样
层级2 (1/4分辨率): 几何一致性迭代
    ↓ 上采样
层级1 (1/2分辨率): 几何一致性迭代
    ↓ 上采样
层级0 (全分辨率): 最终优化
```

每层都进行:
1. 从上层上采样初始化
2. PatchMatch迭代
3. 几何一致性检查

---

### 3.4 几何一致性方法对比

| 方法 | 融合时机 | 计算量 | 精度提升 | 适用场景 |
|-----|---------|-------|---------|---------|
| 后处理(openMVS) | 迭代后 | 低 | 中 | CPU实现 |
| 代价融合(ACMMP) | 迭代中 | 高 | 高 | GPU实现 |
| 多尺度(ACMMP) | 多层级 | 高 | 很高 | 大基线 |

---

## 4. 平面先验 (Planar Prior)

### 4.1 无平面先验 (openMVS)

openMVS不使用显式的平面先验，但使用：

**稀疏点初始化**:
```cpp
// 使用SfM稀疏点的Delaunay三角化来初始化深度
std::pair<float,float> TriangulatePointsDelaunay(
    const DepthData::ViewData& image,
    const PointCloud& pointcloud,
    const IndexArr& points,
    Mesh& mesh,
    Point2fArr& projs)
{
    // 使用CGAL构建Delaunay三角化
    typedef CGAL::Delaunay_triangulation_2<kernel_t, ...> Delaunay;
    Delaunay delaunay;

    for (uint32_t idx: points) {
        const Point3f pt(image.camera.ProjectPointP3(pointcloud.points[idx]));
        delaunay.insert(CPoint(x.x, x.y))->info() = mesh.vertices.size();
        mesh.vertices.emplace_back(image.camera.TransformPointI2C(x));
    }
    // 三角形内插值深度
}
```

---

### 4.2 Delaunay平面先验 (ACMMP)

**原理**: 使用稀疏点构建Delaunay三角化，假设三角形内像素共面。

**ACMMP main.cpp中的流程**:
```cpp
void ACMMP::SetPlanarPrior() {
    // 1. 获取稀疏点
    std::vector<cv::Point2f> points;
    std::vector<float> depths;
    GetSparsePoints(points, depths);

    // 2. Delaunay三角化
    cv::Subdiv2D subdiv(rect);
    for (auto& pt : points)
        subdiv.insert(pt);

    // 3. 对每个三角形，计算平面方程
    std::vector<cv::Vec6f> triangles;
    subdiv.getTriangleList(triangles);

    for (auto& tri : triangles) {
        // 三个顶点的3D坐标
        Point3 p1 = Unproject(tri[0], tri[1], depth1);
        Point3 p2 = Unproject(tri[2], tri[3], depth2);
        Point3 p3 = Unproject(tri[4], tri[5], depth3);

        // 计算平面法向量
        Normal n = normalize(cross(p2-p1, p3-p1));
        float d = -dot(n, p1);

        // 存储为平面先验
        for (像素 in 三角形内) {
            prior_planes[pixel] = make_float4(n.x, n.y, n.z, d);
            plane_masks[pixel] = 1;
        }
    }
}
```

**先验使用** (`ACMMP.cu:673-726`):
```cuda
// 在PlaneHypothesisRefinement中使用先验
if (params.planar_prior && plane_masks[center] > 0) {
    // 从先验平面采样深度
    depth_prior = ComputeDepthfromPlaneHypothesis(cameras[0], prior_planes[center], p);
    depth_rand = curand_uniform(rand_state) * 6 * depth_sigma + (depth_prior - 3 * depth_sigma);

    // 从先验法向量扰动
    plane_hypothesis_rand = GeneratePerturbedNormal(cameras[0], p,
        prior_planes[center], rand_state, angle_sigma);

    // 计算先验惩罚项
    float depth_diff = depths[i] - depth_prior;
    float angle_cos = Vec3DotVec3(prior_planes[center], temp_plane_hypothesis);
    float angle_diff = acos(angle_cos);

    // 高斯权重
    float prior = gamma + exp(-depth_diff² / two_depth_sigma²)
                       * exp(-angle_diff² / two_angle_sigma²);

    // 综合代价
    float restricted_temp_cost = exp(-temp_cost² / beta) * prior;
}
```

**数学公式**:
```
Prior(d, n) = γ + exp(-||d - d_prior||² / 2σ_d²) × exp(-arccos(n·n_prior)² / 2σ_n²)

最终代价 = exp(-C_photo² / β) × Prior(d, n)
```

---

### 4.3 超像素平面先验

**代表方法**: TAPA-MVS

**原理**:
1. 对图像进行超像素分割(SLIC)
2. 假设每个超像素内共面
3. 使用超像素边界作为深度不连续提示

---

### 4.4 平面先验方法对比

| 方法 | 先验来源 | 覆盖率 | 弱纹理处理 | 计算开销 |
|-----|---------|-------|-----------|---------|
| 无先验(openMVS) | - | - | 差 | 无 |
| Delaunay(ACMMP) | 稀疏点 | 部分 | 好 | 中 |
| 超像素 | 图像分割 | 全图 | 好 | 高 |

---

## 5. 匹配代价计算 (Matching Cost)

### 5.1 加权NCC (openMVS)

**openMVS实现** (`DepthMap.cpp:421-500`):
```cpp
// 加权NCC - 使用颜色和空间权重
#if DENSE_NCC == DENSE_NCC_WEIGHTED
bool DepthEstimator::FillPixelPatch()
{
    Weight& w = weightMap0[x0.y*image0.image.width()+x0.x];
    if (w.normSq0 == 0) {
        w.sumWeights = 0;
        int n = 0;
        const float colCenter = image0.image(x0);

        for (int i=-nSizeHalfWindow; i<=nSizeHalfWindow; i+=nSizeStep) {
            for (int j=-nSizeHalfWindow; j<=nSizeHalfWindow; j+=nSizeStep) {
                Weight::Pixel& pw = w.weights[n++];
                // 权重 = 空间高斯 × 颜色高斯
                pw.weight = GetWeight(ImageRef(j,i), colCenter);
                w.normSq0 += image0.image(x0.y+i, x0.x+j) * pw.weight;
                w.sumWeights += pw.weight;
            }
        }
        // 归一化
        const float tm = w.normSq0 / w.sumWeights;
        w.normSq0 = 0;
        n = 0;
        do {
            Weight::Pixel& pw = w.weights[n];
            const float t = pw.tempWeight - tm;
            w.normSq0 += (pw.tempWeight = pw.weight * t) * t;
        } while (++n < nTexels);
    }
    normSq0 = w.normSq0;
}

// 权重计算
float GetWeight(const ImageRef& offset, float centerColor) {
    float spatialWeight = exp(-(offset.x² + offset.y²) / (2 * σ_s²));
    float colorWeight = exp(-|image(x0+offset) - centerColor|² / (2 * σ_c²));
    return spatialWeight * colorWeight;
}
```

**NCC评分**:
```cpp
float DepthEstimator::ScorePixelImage(const ViewData& image1, Depth depth, const Normal& normal)
{
    Matrix3x3f H = ComputeHomographyMatrix(image1, depth, normal);

    float sum = 0, sumSq = 0, num = 0;
    for (int i=-nSizeHalfWindow; i<=nSizeHalfWindow; i+=nSizeStep) {
        for (int j=-nSizeHalfWindow; j<=nSizeHalfWindow; j+=nSizeStep) {
            Point2f pt = WarpPoint(H, x0 + Point2i(j, i));
            float v = image1.image.sample(pt);  // 双线性插值

            const Weight::Pixel& pw = w.weights[n++];
            sum += v * pw.weight;
            sumSq += v * v * pw.weight;
            num += v * pw.tempWeight;
        }
    }

    // NCC = covariance / (σ_ref × σ_src)
    float var_src = sumSq/sumWeights - (sum/sumWeights)²;
    return 1.0 - num / sqrt(normSq0 * var_src);  // 1-NCC作为代价
}
```

---

### 5.2 双边NCC (ACMMP)

**ACMMP实现** (`ACMMP.cu:349-421`):
```cuda
__device__ float ComputeBilateralNCC(
    const cudaTextureObject_t ref_image,
    const Camera ref_camera,
    const cudaTextureObject_t src_image,
    const Camera src_camera,
    const int2 p,
    const float4 plane_hypothesis,
    const PatchMatchParams params)
{
    const float cost_max = 2.0f;
    int radius = params.patch_size / 2;

    // 计算单应矩阵
    float H[9];
    ComputeHomography(ref_camera, src_camera, plane_hypothesis, H);

    float2 pt = ComputeCorrespondingPoint(H, p);
    if (pt.x < 0 || pt.x >= width || pt.y < 0 || pt.y >= height)
        return cost_max;

    float sum_ref = 0, sum_ref_ref = 0;
    float sum_src = 0, sum_src_src = 0;
    float sum_ref_src = 0;
    float bilateral_weight_sum = 0;

    const float ref_center_pix = tex2D<float>(ref_image, p.x + 0.5f, p.y + 0.5f);

    for (int i = -radius; i <= radius; i += params.radius_increment) {
        for (int j = -radius; j <= radius; j += params.radius_increment) {
            const int2 ref_pt = make_int2(p.x + i, p.y + j);
            const float ref_pix = tex2D<float>(ref_image, ref_pt.x + 0.5f, ref_pt.y + 0.5f);

            float2 src_pt = ComputeCorrespondingPoint(H, ref_pt);
            const float src_pix = tex2D<float>(src_image, src_pt.x + 0.5f, src_pt.y + 0.5f);

            // 双边权重 = 空间高斯 × 颜色高斯
            float weight = ComputeBilateralWeight(
                i, j,                    // 空间偏移
                ref_pix, ref_center_pix, // 颜色差
                params.sigma_spatial,    // 空间σ
                params.sigma_color       // 颜色σ
            );

            // 加权统计量
            sum_ref += weight * ref_pix;
            sum_ref_ref += weight * ref_pix * ref_pix;
            sum_src += weight * src_pix;
            sum_src_src += weight * src_pix * src_pix;
            sum_ref_src += weight * ref_pix * src_pix;
            bilateral_weight_sum += weight;
        }
    }

    // 归一化
    sum_ref /= bilateral_weight_sum;
    sum_ref_ref /= bilateral_weight_sum;
    sum_src /= bilateral_weight_sum;
    sum_src_src /= bilateral_weight_sum;
    sum_ref_src /= bilateral_weight_sum;

    // 方差检查（纹理检测）
    const float var_ref = sum_ref_ref - sum_ref * sum_ref;
    const float var_src = sum_src_src - sum_src * sum_src;
    if (var_ref < 1e-5f || var_src < 1e-5f)
        return cost_max;  // 弱纹理

    // NCC
    const float covar = sum_ref_src - sum_ref * sum_src;
    return max(0.0f, min(cost_max, 1.0f - covar / sqrt(var_ref * var_src)));
}
```

**双边权重计算**:
```cuda
__device__ float ComputeBilateralWeight(
    float x_dist, float y_dist,      // 空间距离
    float pix, float center_pix,     // 像素值
    float sigma_spatial,             // 空间σ
    float sigma_color)               // 颜色σ
{
    const float spatial_dist = sqrt(x_dist * x_dist + y_dist * y_dist);
    const float color_dist = fabs(pix - center_pix);

    return exp(-spatial_dist / (2.0f * sigma_spatial * sigma_spatial)
               -color_dist / (2.0f * sigma_color * sigma_color));
}
```

---

### 5.3 其他匹配代价

**Census Transform**:
```cpp
// 比较中心像素与邻域的大小关系，编码为二进制串
uint64_t CensusTransform(Image& img, int x, int y, int radius) {
    uint64_t census = 0;
    float center = img(y, x);
    int bit = 0;
    for (int i = -radius; i <= radius; i++) {
        for (int j = -radius; j <= radius; j++) {
            if (i == 0 && j == 0) continue;
            if (img(y+i, x+j) < center)
                census |= (1ULL << bit);
            bit++;
        }
    }
    return census;
}
// 代价 = Hamming距离
float cost = __popcll(census_ref ^ census_src);
```

**SAD/SSD**:
```cpp
float SAD(Patch& ref, Patch& src) {
    float sum = 0;
    for (int i = 0; i < size; i++)
        sum += abs(ref[i] - src[i]);
    return sum;
}
```

---

### 5.4 匹配代价方法对比

| 方法 | 光照不变性 | 边缘保持 | 计算量 | 弱纹理 |
|-----|-----------|---------|-------|-------|
| NCC | 好 | 差 | 中 | 差 |
| 加权NCC(openMVS) | 好 | 中 | 中 | 中 |
| 双边NCC(ACMMP) | 好 | 好 | 高 | 中 |
| Census | 很好 | 中 | 低 | 中 |
| SAD/SSD | 差 | 差 | 低 | 差 |

---

## 6. 邻域传播策略 (Neighbor Propagation)

### 6.1 4邻域传播 (openMVS)

**实现** (`DepthMap.cpp:660-766`):
```cpp
void DepthEstimator::ProcessPixel(int idx)
{
    neighbors.Empty();

    if (dir == LT2RB) {
        // 左上到右下：使用左、上邻居
        if (x0.x > nSizeHalfWindow) {
            const ImageRef nx(x0.x-1, x0.y);
            if (depthMap0(nx) > 0)
                neighbors.emplace_back(nx);
        }
        if (x0.y > nSizeHalfWindow) {
            const ImageRef nx(x0.x, x0.y-1);
            if (depthMap0(nx) > 0)
                neighbors.emplace_back(nx);
        }
    } else {
        // 右下到左上：使用右、下邻居
        if (x0.x < size.width - nSizeHalfWindow) {
            const ImageRef nx(x0.x+1, x0.y);
            if (depthMap0(nx) > 0)
                neighbors.emplace_back(nx);
        }
        if (x0.y < size.height - nSizeHalfWindow) {
            const ImageRef nx(x0.x, x0.y+1);
            if (depthMap0(nx) > 0)
                neighbors.emplace_back(nx);
        }
    }

    // 从邻居传播最佳假设
    for (auto& neighbor : neighbors) {
        float nconf = confMap0(neighbor.x);
        if (nconf < conf) {
            // 插值邻居深度到当前位置
            Depth ndepth = InterpolatePixel(neighbor.x, neighbor.depth, neighbor.normal);
            float ncost = ScorePixel(ndepth, neighbor.normal);
            if (ncost < conf) {
                conf = ncost;
                depth = ndepth;
                normal = neighbor.normal;
            }
        }
    }
}
```

---

### 6.2 8邻域自适应传播 (ACMMP)

**实现** (`ACMMP.cu:737-942`):
```cuda
__device__ void CheckerboardPropagation(...)
{
    // 8个方向：近邻(4) + 远邻(4)
    // 近邻: 距离1像素
    // 远邻: 距离3像素，但会自适应搜索更远

    int left_near = center - 1;
    int left_far = center - 3;
    int right_near = center + 1;
    int right_far = center + 3;
    int up_near = center - width;
    int up_far = center - 3 * width;
    int down_near = center + width;
    int down_far = center + 3 * width;

    // 自适应远邻居搜索
    // 例如: 向上搜索，在3到23像素范围内找代价最小的点
    if (p.y > 2) {
        costMin = costs[up_far];
        costMinPoint = up_far;
        for (int i = 1; i < 11; ++i) {  // 搜索范围: 3, 5, 7, ..., 23
            if (p.y > 2 + 2 * i) {
                int pointTemp = up_far - 2 * i * width;
                if (costs[pointTemp] < costMin) {
                    costMin = costs[pointTemp];
                    costMinPoint = pointTemp;
                }
            }
        }
        up_far = costMinPoint;  // 自适应选择
    }

    // 近邻居也有自适应搜索
    // 在对角线方向搜索更好的近邻居
    if (p.y > 0) {
        costMin = costs[up_near];
        costMinPoint = up_near;
        for (int i = 0; i < 3; ++i) {
            // 搜索位置: (-1,-1), (-2,-2), (-3,-3) 等对角线
            if (p.y > 1 + i && p.x > i) {
                int pointTemp = up_near - (1 + i) * width - i;
                if (costs[pointTemp] < costMin) {
                    costMin = costs[pointTemp];
                    costMinPoint = pointTemp;
                }
            }
        }
        up_near = costMinPoint;
    }
}
```

**示意图**:
```
自适应8邻域传播:

        远邻居(自适应3-23像素)
              ↑
       . . . * . . .
       . . * . * . .
       . * . . . * .    近邻居(自适应1-4像素对角)
       * . . P . . *  ←→
       . * . . . * .
       . . * . * . .
       . . . * . . .
              ↓
```

---

### 6.3 邻域策略对比

| 策略 | 邻居数量 | 搜索范围 | 适应性 | 收敛速度 |
|-----|---------|---------|-------|---------|
| 4邻域(openMVS) | 4 | 固定1像素 | 差 | 中 |
| 8邻域固定 | 8 | 固定 | 中 | 快 |
| 8邻域自适应(ACMMP) | 8 | 1-23像素 | 好 | 很快 |

---

## 7. 搜索与优化策略 (Search Strategy)

### 7.1 固定范围随机搜索 (openMVS)

**实现** (`DepthMap.cpp:801-852`):
```cpp
void DepthEstimator::ProcessPixel(int idx)
{
    // 根据当前置信度选择搜索范围
    unsigned idxScaleRange = 0;
    if (conf <= thConfSmall)      // 高置信度
        idxScaleRange = 2;        // 小范围精细搜索
    else if (conf <= thConfBig)   // 中置信度
        idxScaleRange = 1;        // 中等范围
    else if (conf >= thConfRand) { // 低置信度
        // 完全随机搜索
        for (unsigned iter = 0; iter < nRandomIters; ++iter) {
            Depth ndepth = RandomDepth(dMinSqr, dMaxSqr);
            Normal nnormal = RandomNormal(viewDir);
            float nconf = ScorePixel(ndepth, nnormal);
            if (conf > nconf) {
                conf = nconf;
                depth = ndepth;
                normal = nnormal;
            }
        }
        return;
    }

    // 在当前估计附近随机搜索
    float scaleRange = scaleRanges[idxScaleRange];
    float depthRange = depth * fRandomDepthRatio;  // 0.3%的深度范围

    Point2f p;
    Normal2Dir(normal, p);  // 转换为角度表示

    for (unsigned iter = 0; iter < nRandomIters; ++iter) {
        // 深度扰动
        Depth ndepth = randomMeanRange(depth, depthRange * scaleRange);
        if (!ISINSIDE(ndepth, dMin, dMax)) continue;

        // 法向量扰动（角度空间）
        Point2f np(
            randomMeanRange(p.x, angle1Range * scaleRange),  // 方位角 ±16°
            randomMeanRange(p.y, angle2Range * scaleRange)   // 仰角 ±10°
        );
        Normal nnormal;
        Dir2Normal(np, nnormal);

        if (nnormal.dot(viewDir) >= 0) continue;  // 确保朝向相机

        float nconf = ScorePixel(ndepth, nnormal);
        if (conf > nconf) {
            conf = nconf;
            depth = ndepth;
            normal = nnormal;
            p = np;
            scaleRange = scaleRanges[++idxScaleRange];  // 缩小搜索范围
        }
    }
}
```

**尺度范围表**:
```cpp
const float scaleRanges[12] = {
    1.0f,      // 初始
    0.5f,      // 第1次找到更好
    0.25f,     // 第2次
    0.125f,    // 第3次
    0.0625f,   // ...
    0.03125f,
    0.015625f,
    0.0078125f,
    0.00390625f,
    0.001953125f,
    0.0009765625f,
    0.00048828125f
};
```

---

### 7.2 多假设并行测试 (ACMMP)

**实现** (`ACMMP.cu:658-735`):
```cuda
__device__ void PlaneHypothesisRefinement(...)
{
    float perturbation = 0.02f;  // 2%扰动

    // 生成多个候选假设
    float depth_rand, depth_perturbed;
    float4 plane_hypothesis_rand, plane_hypothesis_perturbed;

    if (params.planar_prior && plane_masks[center] > 0) {
        // 有平面先验：围绕先验采样
        depth_prior = ComputeDepthfromPlaneHypothesis(...);
        depth_rand = curand_uniform() * 6 * depth_sigma + (depth_prior - 3 * depth_sigma);
        plane_hypothesis_rand = GeneratePerturbedNormal(prior_planes[center], angle_sigma);
    } else {
        // 无先验：全范围随机
        depth_rand = curand_uniform() * (depth_max - depth_min) + depth_min;
        plane_hypothesis_rand = GenerateRandomNormal();
    }

    // 围绕当前估计扰动
    depth_perturbed = curand_uniform() * (1 + perturbation) * depth + ...;
    plane_hypothesis_perturbed = GeneratePerturbedNormal(*plane_hypothesis, perturbation * π);

    // 5个候选假设并行测试
    const int num_planes = 5;
    float depths[5] = {
        depth_rand,      // 随机深度
        *depth,          // 当前深度
        depth_rand,      // 随机深度
        *depth,          // 当前深度
        depth_perturbed  // 扰动深度
    };
    float4 normals[5] = {
        *plane_hypothesis,          // 当前法向 + 随机深度
        plane_hypothesis_rand,      // 随机法向 + 当前深度
        plane_hypothesis_rand,      // 随机法向 + 随机深度
        plane_hypothesis_perturbed, // 扰动法向 + 当前深度
        *plane_hypothesis           // 当前法向 + 扰动深度
    };

    // 评估所有假设，选择最佳
    for (int i = 0; i < num_planes; ++i) {
        float4 temp_hypothesis = normals[i];
        temp_hypothesis.w = GetDistance2Origin(cameras[0], p, depths[i], temp_hypothesis);

        // 计算代价
        ComputeMultiViewCostVector(images, cameras, p, temp_hypothesis, cost_vector);

        float temp_cost = 0;
        for (int j = 0; j < num_images - 1; ++j) {
            if (view_weights[j] > 0) {
                temp_cost += view_weights[j] * cost_vector[j];
                if (params.geom_consistency) {
                    temp_cost += 0.2f * ComputeGeomConsistencyCost(...);
                }
            }
        }
        temp_cost /= weight_norm;

        // 更新最佳假设
        if (temp_cost < *cost) {
            *depth = depths[i];
            *plane_hypothesis = temp_hypothesis;
            *cost = temp_cost;
        }
    }
}
```

---

### 7.3 搜索策略对比

| 策略 | 候选数量 | 搜索方式 | 收敛速度 | 实现复杂度 |
|-----|---------|---------|---------|-----------|
| 固定范围(openMVS) | 6次迭代 | 串行 | 中 | 低 |
| 自适应范围 | 可变 | 串行 | 快 | 中 |
| 多假设并行(ACMMP) | 5个/像素 | 并行 | 快 | 中 |

---

## 8. 多分辨率处理 (Multi-Resolution)

### 8.1 简单上采样 (openMVS)

**原理**: 粗分辨率深度图通过双线性插值上采样到精细分辨率。

```cpp
// openMVS的多分辨率策略
void DenseReconstruction() {
    for (int level = nSubResolutionLevels; level >= 0; level--) {
        float scale = 1.0f / (1 << level);

        // 下采样图像
        cv::resize(image, scaledImage, Size(), scale, scale);

        if (level == nSubResolutionLevels) {
            // 最粗层：随机初始化
            RandomInit(depthMap);
        } else {
            // 其他层：从上层上采样
            cv::resize(prevDepthMap, depthMap, scaledImage.size(),
                       0, 0, INTER_LINEAR);
            depthMap *= 2;  // 深度值翻倍（因为图像放大了）
        }

        // PatchMatch迭代
        for (int iter = 0; iter < nIters; iter++) {
            PatchMatchIteration(scaledImage, depthMap);
        }

        prevDepthMap = depthMap;
    }
}
```

---

### 8.2 联合双边上采样 JBU (ACMMP)

**原理**: 使用全分辨率彩色图像引导低分辨率深度图的上采样，保持边缘对齐。

**ACMMP实现** (`ACMMP.cu:574-640`):
```cuda
// JBU (Joint Bilateral Upsampling)
if (params.upsample) {
    const float scale = 1.0f * params.scaled_cols / width;
    const float sigmad = 0.50f;   // 空间σ
    const float sigmar = 25.5f;   // 颜色σ (范围σ)

    const int Imagescale = max(width / params.scaled_cols, height / params.scaled_rows);
    const int WinWidth = Imagescale * Imagescale + 1;
    int num_neighbors = WinWidth / 2;

    const float o_y = p.y * scale;  // 对应低分辨率坐标
    const float o_x = p.x * scale;

    // 参考图像中心像素（高分辨率）
    const float refPix = tex2D<float>(texture_objects[0].images[0], p.x + 0.5f, p.y + 0.5f);

    float c_total_val = 0.0f;       // 深度加权和
    float normalizing_factor = 0.0f; // 权重和
    float4 n_total_val = {0, 0, 0, 0};  // 法向量加权和

    for (int j = -num_neighbors; j <= num_neighbors; ++j) {
        int r_y = clamp(o_y + j, 0, params.scaled_rows - 1);
        int r_ys = p.y + j;  // 高分辨率y

        for (int i = -num_neighbors; i <= num_neighbors; ++i) {
            int r_x = clamp(o_x + i, 0, params.scaled_cols - 1);
            int r_xs = p.x + i;  // 高分辨率x

            const int s_center = r_y * params.scaled_cols + r_x;

            // 低分辨率深度和法向量
            float srcPix = scaled_plane_hypotheses[s_center].w;
            float4 srcNorm = scaled_plane_hypotheses[s_center];

            // 高分辨率参考图像像素
            float neighborPix = tex2D<float>(texture_objects[0].images[0], r_xs + 0.5f, r_ys + 0.5f);

            // 空间高斯 × 颜色高斯
            float sgauss = SpatialGauss(o_x, o_y, r_x, r_y, sigmad);
            float rgauss = RangeGauss(fabs(refPix - neighborPix), sigmar);
            float totalgauss = sgauss * rgauss;

            // 加权累加
            normalizing_factor += totalgauss;
            c_total_val += srcPix * totalgauss;  // 深度
            n_total_val.x += srcNorm.x * totalgauss;  // 法向量
            n_total_val.y += srcNorm.y * totalgauss;
            n_total_val.z += srcNorm.z * totalgauss;
        }
    }

    // 归一化
    float upsampled_depth = c_total_val / normalizing_factor;
    n_total_val.x /= normalizing_factor;
    n_total_val.y /= normalizing_factor;
    n_total_val.z /= normalizing_factor;
    NormalizeVec3(&n_total_val);

    // 作为当前层的初始化
    plane_hypotheses[center] = n_total_val;
    plane_hypotheses[center].w = upsampled_depth;
}
```

**JBU数学公式**:
```
d_high(p) = Σᵢ w(p, qᵢ) × d_low(qᵢ) / Σᵢ w(p, qᵢ)

其中:
w(p, qᵢ) = G_σs(||p - qᵢ||) × G_σr(|I_high(p) - I_high(q'ᵢ)|)

p: 高分辨率像素位置
qᵢ: 低分辨率邻居位置
q'ᵢ: 高分辨率对应位置
G_σs: 空间高斯核
G_σr: 颜色/范围高斯核
I_high: 高分辨率参考图像
```

**示意图**:
```
低分辨率深度图 (1/4)        高分辨率彩色图像         JBU上采样结果
+---+---+---+               +---------------+        +---------------+
| a | b | c |      +        |   边缘信息    |   =    |  保持边缘的   |
+---+---+---+               |   纹理信息    |        |  深度上采样   |
| d | e | f |               +---------------+        +---------------+
+---+---+---+
```

---

### 8.3 多分辨率方法对比

| 方法 | 边缘保持 | 计算量 | 上采样质量 |
|-----|---------|-------|----------|
| 双线性插值(openMVS) | 差 | 低 | 中 |
| 双三次插值 | 中 | 低 | 中 |
| JBU(ACMMP) | 好 | 高 | 高 |
| 神经网络 | 很好 | 很高 | 很高 |

---

## 9. 深度图融合 (Depth Map Fusion)

### 9.1 几何一致性融合 (openMVS)

**流程**:
```cpp
void FuseDepthMaps() {
    for (每个参考视图 ref) {
        for (每个像素 (u,v) in ref.depthMap) {
            Point3 X = ref.Unproject(u, v, ref.depth(u,v));
            int consistentViews = 0;

            for (每个邻居视图 src) {
                Point2 proj = src.Project(X);
                float expectedDepth = src.camera.Depth(X);
                float actualDepth = src.depthMap(proj);

                if (abs(expectedDepth - actualDepth) / expectedDepth < threshold) {
                    consistentViews++;
                }
            }

            if (consistentViews >= minViews) {
                pointCloud.Add(X, ref.color(u,v), ref.normal(u,v));
            }
        }
    }

    // 去除重复点
    RemoveDuplicates(pointCloud);
}
```

### 9.2 ACMMP融合

**main.cpp中的实现思路**:
```cpp
void RunFusion() {
    // 1. 对每个参考视图
    for (int ref = 0; ref < num_images; ref++) {
        // 2. 读取深度图和法向量图
        cv::Mat depth = cv::imread(...);

        // 3. 对每个像素进行几何一致性检查
        for (int y = 0; y < height; y++) {
            for (int x = 0; x < width; x++) {
                float d = depth.at<float>(y, x);
                if (d <= 0) continue;

                // 3D点
                cv::Point3f pt = Unproject(x, y, d, K, R, t);

                // 检查多视图一致性
                int consistent = 0;
                float depth_sum = d;

                for (int src : neighbors[ref]) {
                    cv::Point2f reproj = Project(pt, K_src, R_src, t_src);
                    float d_src = depth_src.at<float>(reproj);
                    float d_expected = (R_src * pt + t_src).z;

                    if (abs(d_src - d_expected) / d_expected < 0.01f) {
                        consistent++;
                        depth_sum += d_src;
                    }
                }

                if (consistent >= 2) {
                    // 使用平均深度
                    float avg_depth = depth_sum / (consistent + 1);
                    cv::Point3f refined_pt = Unproject(x, y, avg_depth, K, R, t);
                    points.push_back(refined_pt);
                }
            }
        }
    }
}
```

---

## 总结：各模块最佳实践推荐

| 模块 | 推荐方法 | 适用场景 |
|-----|---------|---------|
| **传播模式** | 棋盘格 | GPU实现 |
| **传播模式** | 锯齿形 | CPU实现 |
| **视图选择** | 动态自适应 | 有遮挡场景 |
| **几何一致性** | 代价融合 | 大基线 |
| **平面先验** | Delaunay | 弱纹理区域 |
| **匹配代价** | 双边NCC | 一般场景 |
| **邻域传播** | 8邻域自适应 | 任何场景 |
| **搜索策略** | 多假设并行 | GPU实现 |
| **多分辨率** | JBU | 需要边缘对齐 |

---

## 代码位置快速索引

### openMVS
| 功能 | 文件 | 行号 |
|-----|-----|------|
| 锯齿形映射 | DepthMap.cpp | 329-356 |
| 4邻域传播 | DepthMap.cpp | 660-766 |
| 随机搜索 | DepthMap.cpp | 801-852 |
| 加权NCC | DepthMap.cpp | 421-500 |
| Delaunay初始化 | DepthMap.cpp | 1019-1100 |

### ACMMP
| 功能 | 文件 | 行号 |
|-----|-----|------|
| 棋盘格传播 | ACMMP.cu | 737-999 |
| 双边NCC | ACMMP.cu | 349-421 |
| 几何一致性 | ACMMP.cu | 507-532 |
| 多假设优化 | ACMMP.cu | 658-735 |
| JBU上采样 | ACMMP.cu | 574-640 |
| 动态视图选择 | ACMMP.cu | 423-460 |
